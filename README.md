To run the tool, ensure you Google Cloud API key for Safe Search has been added to check_urls.py and run.sh. Add all of the domains you would like to test to the urls.txt file, then run the script run.sh in your terminal.

Requires:
Python 3.10+ & Requests

Results will be saved as CSV files in the GSF Tool folder.
